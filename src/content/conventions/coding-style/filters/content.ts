import type { ContentBlock } from '@/content/ContentBlock'

export const content: ContentBlock[] = [
  {
    type: 'paragraph',
    text: "A filter control for a table column sits directly above that column's own heading, never in a separate filter bar detached from the table layout. The visual alignment between a filter and its column is what makes the mapping between them obvious at a glance; a detached filter bar forces a reader to guess which column it affects.",
  },
]
